'use client';

/**
 * STUB — replaced in full by the tree screen build (ticket #16, structure
 * from #13): page head, TreeCanvas, legend, zoom bar, drawer mount,
 * empty state. Kept so the route group typechecks while screens are built
 * in parallel.
 */
export default function TreePage() {
  return null;
}
